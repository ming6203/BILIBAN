# BILIBAN - B站用户屏蔽

Chrome 浏览器插件，用于屏蔽 B 站指定用户的评论、视频推荐、搜索结果、动态和弹幕。

## 功能特性

### 📝 核心屏蔽功能
- **评论区屏蔽**：自动隐藏被屏蔽用户的评论和回复
- **视频卡片屏蔽**：首页、搜索页、侧边推荐中隐藏被屏蔽用户的视频
- **搜索结果屏蔽**：搜索页过滤被屏蔽用户的内容
- **动态/弹幕过滤**：API 层面拦截，渲染前过滤
- **主页黑名单提醒**：打开被拉黑用户的空间主页时，自动弹窗提醒并显示所在分组

### 👥 分组与分块管理
- **黑名单分组**：将屏蔽用户按组分类，自由命名，独立开关
- **分块整理**：将相关分组打包为块，便于批量管理和云同步
- **直观管理**：弹窗界面支持对分组和分块的完全控制

### 🚀 一键操作
- **快速屏蔽**：视频卡片和评论区旁注入屏蔽按钮，点击选择分组快速添加
- **批量操作**：支持对分组的批量启用/禁用

### 🔍 评论扫描（v1.1 新增）
- **评论区爬取**：对任意视频（BV 号 / av 号 / 链接）分页拉取全部一级评论，可选展开楼中楼（覆盖率更高）
- **批量匹配黑名单**：自动标记评论用户中已命中黑名单者及其所在分组
- **筛选/标记**：按「已在黑名单 / 未在黑名单 / 分组」筛选，勾选后批量加入分组，一键替代逐条点击屏蔽
- **智能节流**：分页间随机延时 + 最大页数上限，识别风控码（-412/-352/-403）自动降级或停止
- **后台执行**：爬取由 service worker 在后台进行，管理页可随时切换，进度实时同步

### ☁️ 数据同步与备份
- **GitHub Gist 同步**：通过 GitHub Token 备份和同步黑名单到私有/公开 Gist
- **社区订阅**：从公开 GitHub 仓库拉取共享黑名单，自动合并更新
- **分块级同步**：可选择性地推送或拉取特定分块的数据
- **本地备份**：支持 JSON 格式的导入导出，完整保留分组结构

### 🎨 高级特性
- **Shadow DOM 支持**：完全适配 B 站 Web Components 和第三方扩展的 Shadow DOM
- **多扩展兼容**：已适配 BewlyBewly、Bilibili-Gate 等流行扩展
- **暗色模式**：自动适配系统主题，包括按钮样式
- **实时更新**：数据变更后立即生效，无需刷新页面

## 安装方法

### 从源码安装
1. **下载代码**：Clone 或 Download ZIP 本仓库
   ```bash
   git clone https://github.com/ming6203/BILIBAN.git
   ```
2. **打开 Chrome 扩展页面**：访问 `chrome://extensions/`
3. **启用开发者模式**：点击右上角的开发者模式开关
4. **加载扩展**：点击「加载已解压的扩展程序」，选择仓库目录

### 开发注意事项
- **无需构建**：纯原生 JavaScript，直接由 Chrome 加载
- **即时调试**：编辑代码后在扩展页面点击刷新按钮即可生效
- **权限要求**：需要 storage 权限保存设置，activeTab 权限进行页面交互

## 使用指南

### 基本使用
1. **打开面板**：点击浏览器工具栏的插件图标，**直接打开的就是云同步界面**（云端备份 / 社区订阅，弹窗即用）；顶部 ⚙️ 按钮打开完整管理面板，📥/📤 为本地导入导出
2. **创建分组**：在管理面板点击「+ 新建分组」，输入名称，可选择关联到现有分块
3. **添加用户**：输入 B 站用户 UID，选择分组，点击「添加」（或展开分组卡片快速添加）
4. **开启屏蔽**：确保分组开关为开启状态

> 💡 弹窗与完整管理面板的分工：弹窗保持轻量，只负责云同步、订阅拉取、导入导出；分组/分块管理、评论扫描等重逻辑均在管理面板中。

### 分块管理
1. **创建分块**：点击「+ 添加分块」，输入名称和描述
2. **关联分组**：创建分组时选择所属分块，或在分组编辑中修改
3. **批量操作**：点击分块标题可展开/收起关联分组，支持批量开关

### 评论扫描（管理面板 → 评论扫描标签页）
1. **输入视频**：粘贴 BV 号 / av 号 / 视频链接，或点击「使用当前标签页」自动读取正在观看的视频
2. **配置扫描**：选择排序——**热门**（按热度，默认）或**最新**（按发布时间倒序）；选「最新」时可设置**时间范围**（1 秒 ~ N 年，如 `1d`=最近 1 天），只拉取该时间窗内发布的评论，翻页越过边界自动停止；此外可设置最大页数（每页约 20 条，**填 0 = 拉取全部直到评论区结束**），需要更全的覆盖率可勾选「展开楼中楼」；页数/排序/时间范围/楼中楼等设置会自动记忆
3. **开始扫描**：点击「开始扫描」，爬取在后台进行；分页间自动随机延时，可在任意时刻停止
4. **确认结果**：结果以"评论区复原"视图展示——每条评论显示用户名与原文（保留换行），楼中楼回复缩进在下方；评论用户已在黑名单时，名字后直接显示所属分组徽标
5. **搜索置顶**：结果工具栏提供搜索框，输入关键词后命中的评论置顶显示、未命中的沉底置灰（内容仍可见），全选时自动跳过置灰评论，方便"搜索→全选→批量加入"
6. **批量操作**：勾选评论作者后选择目标分组，点击「加入黑名单」一键批量屏蔽；不需要的可以「忽略」
   - **自动锁定**：选择目标分组后，已在**该分组**内的用户会自动锁定（🔒 徽标 + 勾选框禁用），全选时也跳过他们，避免同一分组重复添加

> ⚠️ 频繁大页数扫描可能触发 B 站风控；「填 0 拉全部」时请留意请求量（每页约 20 条评论）；触发风控（-412）时扩展会自动停止并提示。

### 快速屏蔽
- **评论屏蔽**：在评论区的用户头像旁出现的「屏蔽」按钮
- **视频屏蔽**：在视频卡片 UP 主名字旁出现的「屏蔽」按钮
- **主页屏蔽**：访问被屏蔽用户主页时，页面顶部的醒目提示

### GitHub 同步
1. **配置 Token**：
   - 生成 GitHub Token（需要 `gist` 权限）
   - 在弹窗的 GitHub 面板输入并保存
2. **创建/链接 Gist**：
   - Token 验证成功后自动创建 Gist
   - 可切换 Gist 可见性（公开/私有）
3. **分块同步**：
   - **推送**：将选中的分块数据上传到云端
   - **拉取**：从云端下载数据，可选择合并或覆盖
   - **选择性同步**：只同步需要的分块数据

### 社区订阅
1. **添加订阅源**：点击 GitHub 面板的「订阅」标签
2. **配置仓库**：输入 GitHub 仓库信息（如 `owner/repo` 路径）
   ```plaintext
   示例：example-user/shared-blacklist
   ```
3. **拉取分块**：仓库可包含多个分块文件，可选择性拉取
4. **自动更新**：定期从订阅源检查更新

### 扩展兼容性
- **BewlyBewly**：自动检测其 Shadow DOM，内部注入样式和按钮
- **Bilibili-Gate**：适配其视频卡片结构，跳过直播页干扰
- **暗色模式**：按钮样式自动适配系统的明暗主题


## 文件结构
```
├── manifest.json                    # 扩展清单 (MV3, options_ui)
├── background/
│   ├── service-worker.js            # 后台服务：存储变更广播 + 扫描消息路由
│   └── comment-crawler.js           # 评论爬取引擎（WBI 签名、分页、节流、匹配）
├── content/
│   ├── bilibili.js                  # 主内容脚本（DOM 屏蔽 + 按钮注入）
│   ├── interceptor.js               # API 拦截器（请求过滤）
│   └── blocker.css                  # 基础样式
├── lib/
│   ├── storage.js                   # 数据存储层（分组、分块、订阅源）
│   └── github.js                    # GitHub Gist 同步和 Token 管理
├── popup/
│   ├── popup.html                   # 轻量弹窗（云同步/订阅/导入导出）
│   ├── popup.css                    # 弹窗样式
│   └── popup.js                     # 弹窗逻辑
├── manage/
│   ├── manage.html                  # 完整管理面板（黑名单管理 + 评论扫描）
│   ├── manage.css                   # 管理面板样式
│   └── manage.js                    # 管理面板逻辑
└── icons/                           # 扩展图标
    ├── icon16.png
    ├── icon48.png
    └── icon128.png
```

## API 拦截原理
### 请求过滤
插件在 `document_start` 阶段拦截所有 fetch 和 XMLHttpRequest 请求：
1. **预处理响应**：监听特定 API 端点（评论、动态、弹幕、搜索、直播等）
2. **UID 提取**：解析响应 JSON 中的用户数据
3. **条件过滤**：
   - 评论接口：过滤屏蔽用户的评论和回复
   - 动态接口：过滤屏蔽用户的动态
   - 弹幕接口：过滤屏蔽用户的弹幕
   - 搜索结果：过滤屏蔽用户的视频
4. **修改响应**：移除被屏蔽用户的数据后返回给页面

## 兼容性适配

### 适配的增强扩展
1. **[BewlyBewly](https://github.com/BewlyBewly/BewlyBewly)**
   - 自动检测并绕过其 Shadow DOM
   - 按钮样式适配暗色模式
   - 支持其自定义布局和主题

2. **[Bilibili-Gate](https://github.com/nichuanfang/Bilibili-Gate)**
   - 适配 Gate 的 `.bilibili-gate-video-grid` 布局
   - 正确处理视频卡片结构
   - 跳过直播页面避免冲突


## 许可协议

MIT License


## 常见问题

### Q: 插件不生效怎么办？
A: 
1. 确认扩展已正确加载（chrome://extensions/ 中显示为已启用）
2. 确认有启用的屏蔽分组
3. 尝试刷新 B 站页面

### Q: 如何获取用户 UID？
A:
1. 打开用户个人主页，URL 中的数字就是 UID，例如 `https://space.bilibili.com/123456`
2. 用户 UID 为：`123456`
3. 插件自动识别页面中的 UID，一键屏蔽无需手动输入

### Q: GitHub 同步失败？
A:
1. 确认 Token 有 `gist` 权限
2. 检查网络连接是否能访问 GitHub
3. 确认 Gist 可见性设置正确
4. 查看浏览器控制台（F12）的错误信息

### Q: 如何迁移到新设备？
A:
1. 在旧设备导出 JSON 备份
2. 在新设备安装插件
3. 导入备份文件
4. 或配置 GitHub Token 进行云同步